---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: saas-customer-portal
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: saas-customer-portal
          env:
            - name: ENVIRONMENT
              value: "development"
            - name: DEBUG
              value: "true"
            - name: LOG_LEVEL
              value: "DEBUG"
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "250m"
